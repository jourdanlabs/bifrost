{
  "name": "@bifrost/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Mobile-first BIFROST verifier PWA.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs --watch",
    "test": "node scripts/smoke.mjs",
    "serve": "npx serve dist -l 8792"
  },
  "dependencies": {
    "@bifrost/types": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.21.0"
  }
}
