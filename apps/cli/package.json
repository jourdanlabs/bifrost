{
  "name": "bifrost",
  "version": "0.1.0",
  "private": false,
  "description": "AI generates. BIFROST verifies. — CLI client.",
  "bin": {
    "bifrost": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/index.js",
    "dev": "ts-node-dev --transpile-only src/index.ts"
  },
  "dependencies": {
    "@bifrost/types": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.0"
  }
}
